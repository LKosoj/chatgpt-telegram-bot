[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function 
testpaths = tests bot/tests
markers =
    llm_judge: LLM-as-judge eval (needs network + OPENAI_API_KEY + RUN_LLM_JUDGE_EVALS=1; see evals/README.md)